#include "SWITCH.h"
#include "GPIO.h"

void SWITCH_init(void)
{
    GPIO_Pin_t* Pin;
    uint8_t LOC_Counter = 0;

    for(LOC_Counter = 0; LOC_Counter < _SWITCH_NO; LOC_Counter++)
    {
        Pin
    }
}